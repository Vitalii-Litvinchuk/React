const intialState = {
    ContactList: [],
    SearchValue: "",
    CurrentContact: "",
    IsRequest: true,
}

const ListReducer = (state = intialState, action) => {
    return state;
}

export default ListReducer